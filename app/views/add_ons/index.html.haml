
#header
  %h1
    Package Amenity
  = link_to [:new, current_hotel, :add_on], :class=>'create' do
    %i.icon-plus
    Create Amenity

#data
  Package Amenities can be added to existing room types to create sellable Packages.
  %table.standard
    %thead
      %tr
        %th Name
        %th Description
        %th Price
        %th
    %tbody
      - @add_ons.each do |add_on|
        = render "/add_ons/index/row", add_on: add_on
        
    - if @add_ons.total_pages > 1
      %tfoot
        %tr
          %td{colspan: 4}
            = will_paginate @add_ons
        
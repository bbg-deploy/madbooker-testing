#header
  %h1
    Guest Check-In

#data
  #calendar
    #date_picker
          
  - if @bookings.blank?
    No Reservations for 
    = params[:date]
   
  #today
    - @bookings.each do |booking|
      = render "/check_ins/index/booking", booking: booking

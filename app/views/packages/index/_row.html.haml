
- package ||= @package

%tr{id: dom_id( package)}
  %td= package.room_name
  %td= package.description.truncate 30
  %td= package.additional_price
  %td
    = link_to "Edit", [:edit, current_hotel, package]
    = package.activate_link